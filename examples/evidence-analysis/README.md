# Evidence analysis example

Run `npm run demo` from the repository root. The command writes a deterministic
world artifact and a normalized evidence bundle under `fixtures/`.

The evidence bundle is research input only. It does not establish a legal,
regulatory, audit, or compliance conclusion.
